include:
  - grafana/grafana.yaml

services:
  valkey:
    image: valkey/valkey:9.0.3
    ports:
      - "6379:6379"
    volumes:
      - valkey_data:/data
    command: ["valkey-server", "--save", "60", "1", "--loglevel", "warning"]
    restart: always

volumes:
  valkey_data:
